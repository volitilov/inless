
var routes = require('./../../../configs/routes.json');

var Router = require('./router.js');

var router = new Router;

var ___ = function() {
	require('./../../../application/routes/**/react/handler.jsx', { glob: true });
};



routes.forEach(function(e) {
	var {path, status, title, name} = e;
	var handler = require('./../../../application/routes/'+name+'/react/handler.jsx');
	var express = null;
	router.add(path, handler, status, title, express);
});




module.exports = router;
